import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#EB4C00",
            dark: "#361C00"
        },
        secondary: {
            main: "#F2A900",
        },
    },
    typography: {
        fontFamily: '"IBM Plex Serif"',
    },
});
